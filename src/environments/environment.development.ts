/**
 * Environment de desenvolvimento local.
 *
 * Aplicado por `ng serve` / `npm start` (config `development`), substituindo
 * `environment.ts` via `fileReplacements` (ver angular.json).
 */
export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080',
};
